import { Section } from '../..'
import styles from './Profile.module.scss'

const Profile: React.FC = () => {
  return <Section className={styles.Profile} title="Profile"></Section>
}

export default Profile
